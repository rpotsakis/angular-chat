'use strict';

describe('chatDetails module', function() {

  beforeEach(module('myApp.chatDetails'));

  describe('ChatDetails controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var chatDetailsCtrl = $controller('ChatDetailsCtrl');
      expect(chatDetailsCtrl).toBeDefined();
    }));

  });
});