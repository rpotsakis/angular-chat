'use strict';

describe('myApp.chatList module', function() {

  beforeEach(module('myApp.chatList'));

  describe('chatList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var ChatListCtrl = $controller('ChatListCtrl');
      expect(ChatListCtrl).toBeDefined();
    }));

  });
});